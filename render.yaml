services:
  - type: web
    name: vehicle-service-management
    dockerfile: Dockerfile
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: FLASK_ENV
        value: production

